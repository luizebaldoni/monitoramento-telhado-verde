# Exemplo de .env para a API FastAPI
# Defina o caminho para o arquivo de credenciais do Firebase (Service Account JSON)
FIREBASE_CREDENTIALS_PATH=config/firebase-credentials.json

# Porta e host (opcional)
# API_HOST=0.0.0.0
# API_PORT=8000

# Opcional: API_KEY para autenticação (se você adicionar suporte)
# API_KEY=troque_por_uma_chave_segura

