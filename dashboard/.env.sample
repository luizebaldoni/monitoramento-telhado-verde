# Exemplo de variáveis de ambiente para o dashboard Streamlit
# Habilite o consumo via API definindo USE_API=1
# Defina API_URL para o endereço base da sua API FastAPI (sem sufixo /sensor-data)

# USE_API=1  # Habilita uso da API FastAPI (quando definido como 1/true)
# API_URL=http://127.0.0.1:8000

# Se você preferir acessar diretamente o Firestore, deixe USE_API desabilitado
# FIREBASE_CREDENTIALS_PATH=config/firebase-credentials.json

